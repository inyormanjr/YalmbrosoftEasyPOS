import { environment } from 'src/environments/environment';
const _token = 'token';
const _username = 'username';
const _role = 'role';
const _company = 'company';
const _userObj = 'UserObj';
const _profilePhotoBaseURL = environment.baseProfilePhotoURL;

export { _token, _username, _role, _company, _userObj, _profilePhotoBaseURL };
